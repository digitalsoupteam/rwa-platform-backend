{
  "name": "@services/blockchain-scanner",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "bun run --watch src/index.ts",
    "start": "bun run src/index.ts",
    "build": "tsc"
  },
  "dependencies": {
    "@elysiajs/eden": "latest",
    "@shared/elysia-plugins": "workspace:*",
    "@shared/monitoring": "workspace:*",
    "@shared/errors": "workspace:*",
    "@shared/rabbitmq": "workspace:*",
    "amqplib": "latest",
    "elysia": "latest",
    "ethers": "latest",
    "mongoose": "latest"
  },
  "devDependencies": {
    "@types/amqplib": "latest",
    "bun-types": "latest"
  }
}