scalar JSON

enum SortDirection {
  asc
  desc
}

input SortFieldInput {
  field: String!
  direction: SortDirection!
}

input PaginationInput {
  limit: Int
  offset: Int
  sort: SortFieldInput
}

type SocialLink {
  type: String!
  url: String!
}

input SocialLinkInput {
  type: String!
  url: String!
}

type Query {
  _: Boolean
}

type Mutation {
  _: Boolean
}

type Subscription {
  _: Boolean
}