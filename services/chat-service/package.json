{
  "name": "@rwa-platform/chat-service",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "bun run --watch src/server.ts",
    "build": "tsc && bun build ./src/server.ts --outdir ./dist --target bun",
    "start": "bun run dist/server.js",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@elysiajs/cors": "^1.2.0",
    "@elysiajs/swagger": "^1.2.0",
    "@rwa-platform/shared": "workspace:*",
    "elysia": "^1.2.12",
    "mongoose": "^8.10.0"
  },
  "devDependencies": {
    "bun-types": "latest"
  }
}
