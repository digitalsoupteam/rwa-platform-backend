# Grafana
server {
    listen 443 ssl;
    server_name grafana.rwa.local;
    ssl_certificate /etc/nginx/ssl/nginx.crt;
    ssl_certificate_key /etc/nginx/ssl/nginx.key;
    ssl_protocols TLSv1.2 TLSv1.3;

    location / {
        auth_request /authelia/api/verify;
        error_page 401 =302 https://auth.rwa.local/;
        auth_request_set $user $upstream_http_remote_user;
        proxy_set_header Remote-User $user;
        
        set $grafana_upstream "http://grafana:3000";
        proxy_pass $grafana_upstream;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $http_host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    include /etc/nginx/snippets/authelia-verify.conf;
}

# Prometheus
server {
    listen 443 ssl;
    server_name prometheus.rwa.local;
    ssl_certificate /etc/nginx/ssl/nginx.crt;
    ssl_certificate_key /etc/nginx/ssl/nginx.key;
    ssl_protocols TLSv1.2 TLSv1.3;

    location / {
        auth_request /authelia/api/verify;
        error_page 401 =302 https://auth.rwa.local/;
        
        set $prometheus_upstream "http://prometheus:9090";
        proxy_pass $prometheus_upstream;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    include /etc/nginx/snippets/authelia-verify.conf;
}

# Uptime Kuma
server {
    listen 443 ssl;
    server_name uptime.rwa.local;
    ssl_certificate /etc/nginx/ssl/nginx.crt;
    ssl_certificate_key /etc/nginx/ssl/nginx.key;
    ssl_protocols TLSv1.2 TLSv1.3;

    location / {
        auth_request /authelia/api/verify;
        error_page 401 =302 https://auth.rwa.local/;
        
        set $uptime_kuma_upstream "http://uptime-kuma:3001";
        proxy_pass $uptime_kuma_upstream;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_buffering off;
    }

    include /etc/nginx/snippets/authelia-verify.conf;
}

# Alloy
server {
    listen 443 ssl;
    server_name alloy.rwa.local;
    ssl_certificate /etc/nginx/ssl/nginx.crt;
    ssl_certificate_key /etc/nginx/ssl/nginx.key;
    ssl_protocols TLSv1.2 TLSv1.3;

    location / {
        auth_request /authelia/api/verify;
        error_page 401 =302 https://auth.rwa.local/;
        
        set $alloy_upstream "http://alloy:12345";
        proxy_pass $alloy_upstream;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    include /etc/nginx/snippets/authelia-verify.conf;
}
