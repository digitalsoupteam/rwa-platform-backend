{
  "name": "rwa-platform",
  "private": true,
  "workspaces": [
    "shared/*",
    "services/*"
  ],
  "scripts": {
    "app:up": "docker compose -f infrastructure/docker/docker-compose.yml up -d",
    "nginx:reload": "docker compose -f infrastructure/docker/docker-compose.yml exec nginx sh -c \"envsubst '\\$BASE_DOMAIN' < /etc/nginx/templates/default.conf.template > /etc/nginx/conf.d/default.conf && nginx -s reload\""
  },
  "devDependencies": {
    "@types/bun": "1.2.19"
  }
}
