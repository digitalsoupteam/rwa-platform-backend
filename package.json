{
  "name": "rwa-platform",
  "private": true,
  "workspaces": [
    "shared/*",
    "services/*"
  ],
  "scripts": {
    "app:up": "docker compose -f infrastructure/docker/docker-compose.yml up -d",
    "nginx:reload": "docker compose -f infrastructure/docker/docker-compose.yml exec nginx sh -c \"envsubst '\\\\$BASE_DOMAIN,\\\\$NGINX_SSL_CERT_PATH,\\\\$NGINX_SSL_KEY_PATH,\\\\$FILES_URL_PATH' < /etc/nginx/templates/default.conf.template > /etc/nginx/conf.d/default.conf && nginx -s reload\"",
    "format": "prettier --write \"services/**/*.ts\" \"shared/**/*.ts\""
  },
  "devDependencies": {
    "@types/bun": "1.2.19",
    "prettier": "^3.8.5"
  }
}